[project]
name = "manga-polyglot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "torch",
    "transformers",
    "Pillow",
    "tqdm",
    "scipy",
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "numpy",
    "scikit-learn",
    "jaconv",
    "ollama",
    "onnxruntime",
]

[project.optional-dependencies]
dev = [
    "pytest",
]
